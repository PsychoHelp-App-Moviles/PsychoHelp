<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codetech.psychohelp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PsychoHelp"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".HistoryAppointmentsPsychologist"
            android:exported="false" />

        <activity android:name=".Publication.ListPublication"
            android:exported="false"/>

        <activity
            android:name=".psychologist.PsychoProfile"
            android:exported="false" />
        <activity
            android:name=".patient.PatientProfile"
            android:name=".PsychologistActivity"
            android:exported="false" />
        <activity
            android:name=".PatientActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>